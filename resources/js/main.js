// document.querySelectorAll('*').forEach(el => {
//   el.style.outline = '1px solid rgba(255,0,0,.2)';
// });
// Initialization for ES Users


var splide = new Splide( '.splide', {
  type   : 'loop',
  padding: '5rem',
} );

splide.mount();